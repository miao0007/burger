<div class="container-fluid">
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8 mt-5">
            <h1 align="center" class="mb-4 title">EAT DA BURGER</h1>
            <img align="center" id="burgerPicture" src="/assets/img/burger.jpg" alt="hamburger" class="mx-auto d-block">
            
        </div>
        <div class="col-sm-2"></div>
    </div>
    <div class="row burgerArea">
        <div class="col-sm-2"></div>
        <div class="col-sm-4 mt-2">
            <h3 align="center">Hamburger Menu</h3>
            <ul class="list-group">
                {{#each hamburgers}}
                    {{#unless devoured}} 
                        <li class="list-group-item">
                            <p class="d-inline-flex my-2">
                                {{burger_name}}</p>
                            <button class="devourBtn btn btn-success float-right" data-id="{{id}}">DEVOUR IT!</button>
                        </li>
                    {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-sm-4 mt-2">
            <h3 align="center">Devoured Hamburgers</h3>
            <ul class="list-group">
            {{#each hamburgers}}
                {{#if devoured}}
                    <li class="list-group-item">
                        <p class="d-inline-flex my-2">
                            {{burger_name}}</p>
                        <button class="deleteBtn btn btn-danger float-right" data-id="{{id}}">DELETE</button>
                    </li>
                {{/if}}
            {{/each}}
            </ul>
        </div>
        <div class="col-sm-2"></div>
    </div>
    <div class="row mt-4">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">
            <h3 align="center">Add a Burger You Want To Eat</h3>
            <form class="create-form">
                <div class="form-group mx-auto" align="center">
                    <div class="input-group mb-3">
                        <input type="text" class="form-control" placeholder="Burger name" aria-label="Recipient's username" aria-describedby="basic-addon2" id="burgerName">
                        <div class="input-group-append">
                            <button class="btn btn-warning addBtn" type="submit">ADD To Menu</button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
        <div class="col-sm-2">
        </div>
    </div>

    <!-- Footer -->
    <div class="navbar fixed-bottom justify-content-center">&copy; App by Jeff Miao</div>

</div>